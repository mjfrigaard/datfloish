% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch_app.R
\name{launch_app}
\alias{launch_app}
\title{Launch the datfloish Shiny Application}
\usage{
launch_app(
  db_path = system.file("extdata", "sales.db", package = "datfloish"),
  interval_ms = 5000,
  ...
)
}
\arguments{
\item{db_path}{Path to the SQLite database file. Defaults to the bundled
`sales.db` in `inst/extdata/` via [base::system.file()].}

\item{interval_ms}{Poll interval in milliseconds passed to
[mod_sales_table_server()] and [mod_value_boxes_ui()]. Default `5000`.}

\item{...}{Additional arguments passed to [shiny::shinyApp()].}
}
\value{
A Shiny app object (invisibly when run interactively).
}
\description{
Convenience wrapper around [shiny::shinyApp()] that wires together
[app_ui()] and [app_server()]. Run [seed_db()] first if the database
does not yet exist.
}
\examples{
\dontrun{
launch_app()   # launch the app using the bundled sales.db
}
}
