# Development entry point.
# Use this file with shiny::runApp() or the RStudio "Run App" button.
# Loads the package source without requiring a full install.
pkgload::load_all(quiet = TRUE)

launch_app()
